// ./src/components/LoginImage.js
import React from 'react';
import { Image } from 'react-native';

const LoginImage = ({ source }) => {
  return <Image source={source} />;
};

export default LoginImage;
