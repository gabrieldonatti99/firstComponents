// ./src/components/LoginText.js
import React from 'react';
import { Text } from 'react-native';

const LoginText = ({ children }) => {
  return <Text>{children}</Text>;
};

export default LoginText;
